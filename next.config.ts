import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/Gujarat-SVG-Component',
  assetPrefix: '/Gujarat-SVG-Component',
  trailingSlash: true,
};

export default nextConfig;